import { solveDayN } from "./dayN";

describe("Day N", () => {
  it("should solve example input correctly", () => {
    const exampleInput = `example input here`;
    expect(solveDayN(exampleInput)).toBe(0);
  });
});
